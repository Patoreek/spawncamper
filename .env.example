# Database (SQLite) — relative paths are anchored to the workspace root,
# so every package opens the same file. Use an absolute path if you want
# to override that.
SPAWNCAMPER_DB='spawncamper.db'

# Amazon Product Advertising API (PA-API v5)
# Sign up: https://affiliate-program.amazon.com
# Credentials: https://affiliate-program.amazon.com/assoc_credentials/home
AMAZON_ACCESS_KEY=''
AMAZON_SECRET_KEY=''
AMAZON_PARTNER_TAG=''

# ScraperAPI — proxy for heavily protected sites (Big W, eBay, Best Buy, etc.)
# Sign up: https://www.scraperapi.com (free tier: 5,000 requests/month)
SCRAPER_API_KEY=''

# Telegram notifications
# Create a bot via @BotFather to get the token, then DM your bot and use
# https://api.telegram.org/bot<TOKEN>/getUpdates to find your chat id.
# If either is unset, notifications are skipped with a console warning.
TELEGRAM_BOT_TOKEN=''
TELEGRAM_CHAT_ID=''

# Daily digest — a single Telegram summary of every active product. Opt-in.
# DIGEST_CRON is a 5-field cron expression evaluated in DIGEST_TZ. Unset =
# no digest. DIGEST_TZ defaults to Australia/Sydney (matches price-check tz).
# Example: DIGEST_CRON='0 8 * * *' for 8am every day.
DIGEST_CRON=''
DIGEST_TZ=''
